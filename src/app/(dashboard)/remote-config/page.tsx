import { PageHeader } from "@/components/page-header";
export default function RemoteConfig(){return <><PageHeader title="Remote configuration" detail="Versioned, audited values for non-sensitive optional behavior. Invalid or unavailable config fails open to phone-local defaults." actions={<div className="page-actions"><button className="secondary">Preview JSON</button><button className="primary">Add setting</button></div>}/><div className="grid info-grid"><article className="card"><div className="metric-title">Allowed types</div><div className="metric-value">5</div><div className="metric-help">String, number, boolean, JSON, and enum — validated before publish.</div></article><article className="card"><div className="metric-title">Core data protection</div><div className="metric-value">Locked</div><div className="metric-help">Task, timer, timeline, reminder, and SQLite controls are rejected.</div></article><article className="card"><div className="metric-title">Published settings</div><div className="metric-value">0</div><div className="metric-help">No public configuration payload yet.</div></article></div><section className="card" style={{marginTop:18}}><div className="empty">Remote config starts empty by design. Add a documented optional key, compatibility range, local default, and audit-safe description.</div></section></>}
