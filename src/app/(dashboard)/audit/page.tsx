import { PageHeader } from "@/components/page-header";
export default function Audit(){return <><PageHeader title="Audit & security" detail="Immutable, UTC-stamped records for access and privileged control-plane changes." actions={<button className="secondary">Export CSV</button>}/><div className="grid info-grid"><article className="card"><div className="metric-title">RLS coverage</div><div className="metric-value">Defined</div><div className="metric-help">All exposed control-plane tables are default-deny.</div></article><article className="card"><div className="metric-title">Function access</div><div className="metric-value">Restricted</div><div className="metric-help">Public execute is revoked from privileged RPCs.</div></article><article className="card"><div className="metric-title">Audit stream</div><div className="metric-value">Empty</div><div className="metric-help">No sensitive values are stored in metadata.</div></article></div></>}
