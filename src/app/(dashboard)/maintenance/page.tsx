import { PageHeader } from "@/components/page-header";
export default function Maintenance(){return <><PageHeader title="Service maintenance" detail="A maintenance window only affects optional remote services. DayTrace still opens and uses its local database offline." actions={<button className="primary">Schedule maintenance</button>}/><div className="offline-banner">Maintenance cannot block phone-local tasks, timers, reminders, timeline, reports, or backup export/import.</div><section className="card"><div className="empty">No active or scheduled optional-service maintenance windows.</div></section></>}
