import { PageHeader } from "@/components/page-header";
export default function Settings(){return <><PageHeader title="Platform settings" detail="Owner-only integration metadata and safe defaults. Secret values are never rendered in this dashboard."/><div className="grid info-grid"><article className="card"><div className="heading-row"><h2>Admin allowlist</h2></div><p className="metric-help">Cannot remove the final owner. Database policy also prevents lockout.</p><button className="secondary">Manage owners</button></article><article className="card"><div className="heading-row"><h2>Environment status</h2></div><p className="metric-help">Supabase URL, publishable key, service role, GitHub, and optional AI proxy are shown only as configured/missing.</p><button className="secondary">Review status</button></article><article className="card"><div className="heading-row"><h2>Backup retention</h2></div><p className="metric-help">Controlled download/delete uses typed confirmation and immutable audit logging.</p><button className="secondary">Configure policy</button></article></div></>}
