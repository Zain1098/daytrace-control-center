import { PageHeader } from "@/components/page-header";
export default function Backups(){return <><PageHeader title="Backup inspection" detail="Explicit owner uploads only. Files are validated and inspected; they are never restored to a phone automatically." actions={<button className="primary">Upload for inspection</button>}/><section className="card"><div className="heading-row"><h2>Private backup storage</h2><span className="status warn">Not configured</span></div><div className="empty">Accepted files must contain format <code>daytrace-backup</code>, schemaVersion, createdAtUtc, and all expected records. Uploads receive a checksum, retention state, warnings, and an audit entry.</div></section></>}
