import { PageHeader } from "@/components/page-header";
export default function Health(){return <><PageHeader title="System health" detail="Live checks appear only once the corresponding integration is configured." actions={<button className="primary">Run checks</button>}/><section className="card">{["Vercel deployment","Dashboard API routes","Supabase database","Private Storage","GitHub release metadata","Remote configuration endpoint","Optional AI proxy"].map(name=><div className="health-row" key={name}><span className="health-name">{name}</span><span className="status warn">Awaiting setup</span></div>)}</section></>}
