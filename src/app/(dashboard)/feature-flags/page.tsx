import { PageHeader } from "@/components/page-header";
export default function FeatureFlags(){return <><PageHeader title="Feature flags" detail="Optional services only. The essential offline planner, timer, and local database can never be disabled remotely." actions={<button className="primary">Create flag</button>}/><section className="card"><div className="heading-row"><h2>Flag registry</h2><span className="status warn">0 active</span></div><div className="empty">Suggested optional-service flags: AI summary, update checker, backup upload, experimental UI, diagnostics. Scheduling, retirement, rollback, and every change are audited.</div></section></>}
